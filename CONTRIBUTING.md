# แนวทางการพัฒนาต่อ

## กติกาข้อเดียวที่สำคัญที่สุด

**แก้ pattern แล้วต้องรัน `npm test` ทุกครั้ง**

```bash
npm test
```

ถ้าบรรทัด `ผิดข้างแบบอันตราย` มากกว่า 0 แปลว่ามีข้อความสแกมที่ระบบตอบว่าเป็นข้อความปกติ — อย่า commit จนกว่าจะแก้ได้ (`npm test` คืน exit code 1 ให้แล้ว เอาไปต่อ CI ได้ แต่ยังไม่ได้ตั้ง workflow ไว้ในโปรเจกต์)

## รูปแบบการทำงานที่แนะนำ

| อยากทำอะไร | ทำที่ไหน |
|---|---|
| เพิ่ม/แก้คำ ทดลองเร็ว ๆ | เปิด `admin.html` แก้แล้วดูผลเทียบก่อน-หลัง จบแล้วดาวน์โหลดมาทับ `patterns.json` |
| แก้ตรรกะการตัดสิน | `engine.js` |
| แก้หน้าตา / ข้อความ | `index.html` (ข้อความทุกภาษาอยู่ในตัวแปร `T`) |
| แก้สูตรวัดผล | `test-core.js` — มีที่เดียว ทั้ง CLI และ admin ใช้ร่วมกัน |
| เพิ่มเคสทดสอบ | `tests/testset.json` |

## เพิ่มเคสทดสอบเมื่อไร

ทุกครั้งที่เจอข้อความที่ระบบตอบผิด ให้เพิ่มลง `tests/testset.json` **ก่อน** แก้ pattern แล้วค่อยแก้ให้ผ่าน วิธีนี้ทำให้เคสนั้นไม่ย้อนกลับมาพังอีก

เลือกชุดให้ถูก

- `holdout_bank_real` — ข้อความจริงจากธนาคาร ที่ต้องไม่ถูกตีว่าสแกม
- `holdout_scam_th` / `holdout_scam_en` — สแกมจริงที่ต้องจับได้
- `adversarial` — สแกมที่จงใจเลี่ยงคำ **อย่าไล่แก้ pattern ให้ผ่านชุดนี้ทีละข้อ** เพราะจะกลายเป็นการจำข้อสอบ ใช้ดูแนวโน้มอย่างเดียว
- `evasion` — เทคนิคหลบที่โจมตีตรรกะของ engine ไม่ใช่ช่องว่างของคลังคำ **ชุดนี้ต้องผ่านทุกข้อ** และ **ห้ามแก้ด้วยการเพิ่ม pattern** เพราะคำที่ใช้มีอยู่ในคลังอยู่แล้ว ต้องแก้ที่ `engine.js`
- `holdout_ambiguous` — ข้อความกำกวมที่ควรตอบว่ายืนยันไม่ได้
- `regression_v03` — ชุดเดิม อย่าเพิ่มของใหม่เข้าไป และห้ามใช้ตัวเลขจากชุดนี้อ้างความแม่นยำ

## ตั้งน้ำหนักอย่างไร

| น้ำหนัก | ใช้กับ | ตัวอย่าง |
|---|---|---|
| 1 | คำที่พบในข้อความปกติได้บ่อย | `urgent`, `ด่วน` |
| 2–3 | บ่งชี้ได้แต่ไม่เด็ดขาด | `กดลิงก์`, `พาร์ทไทม์` |
| 4–5 | แทบไม่พบในข้อความจริง | `ไม่เช็คบูโร`, `send your otp` |

ตัวอย่างที่พลาดง่าย: ใส่คำว่า `ชำระ` น้ำหนัก 5 จะทำให้ข้อความจริง "ชำระเงินเรียบร้อย ค่าน้ำประปา 285 บาท" ถูกตีว่าสแกมทันที คำที่กว้างเกินไปต้องได้น้ำหนักต่ำ หรือเขียนให้ยาวขึ้นจนเฉพาะเจาะจง

## ก่อน commit

```bash
npm test        # ต้องไม่มี hard miss
node build.js   # ต้อง build standalone ผ่าน
```

## ห้ามเด็ดขาด

- อย่า commit `.env` หรือ API key ลง repo (มีใน `.gitignore` แล้ว แต่ตรวจซ้ำก่อน push ทุกครั้ง)
- อย่าใส่ API key ลงใน `index.html` หรือไฟล์ใด ๆ ฝั่ง client — key ต้องอยู่ใน `ocr-proxy` เท่านั้น
- อย่าลบข้อความเตือนผู้ใช้ในหน้าเว็บ ทั้งส่วน "เครื่องมือนี้ตัดสินอย่างไร" และข้อความแจ้งเรื่องการส่งภาพออกไปประมวลผล
- อย่าเขียนข้อความ privacy แบบตายตัว — โหมด Typhoon ส่งภาพออกนอกเครื่องจริง ข้อความทุกจุดที่พูดเรื่องนี้ต้องสลับตาม `OCR.proxyUrl` ไม่ใช่แค่กล่องเตือนอันเดียว
- อย่าเปลี่ยน `OCR.cdn` โดยไม่คำนวณ `cdnIntegrity` ใหม่ และอย่าถอด SRI ออกเพื่อให้มันโหลดผ่าน

## สิ่งที่ยังไม่ได้ทำ

ดูหัวข้อ "ที่ยังไม่ได้ทำ" ใน `README.md`
